# Primary database connection string used by the application and Drizzle migrations.
# Provide either DATABASE_URL directly or the Supabase specific variables below.
DATABASE_URL=

# Set to "true" to allow the server to fall back to an in-memory demo database
# when the configured PostgreSQL instance is unreachable (recommended for local
# development in constrained environments).
ALLOW_OFFLINE_DATABASE=true

# --- Supabase helper variables -------------------------------------------------
# If DATABASE_URL is left blank, the application will attempt to build one by
# using the values below. They map directly to the fields found in the Supabase
# project settings under "Configuration".
# Required when using the helper:
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=

# Optional Supabase API configuration for client/server usage
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional overrides (defaults shown in comments):
# SUPABASE_DB_HOST=db.${SUPABASE_PROJECT_REF}.supabase.co
# SUPABASE_DB_PORT=5432
# SUPABASE_DB_USER=postgres
# SUPABASE_DB_NAME=postgres
# SUPABASE_DB_PARAMS=sslmode=require&pgbouncer=true&connection_limit=1

# --- AI providers --------------------------------------------------------------
# Required for AI-assisted generators. Leave blank to disable these endpoints
# locally; the server will respond with a 503 error when invoked.
OPENAI_API_KEY=
